-- VELOCITY LOCK (Triggered by specific moves: Diamond M1 & Ghoul Grab) – ROG ON TOP
-- Tap ⚡ to open/close GUI, drag ⚡ to reposition it.

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local player = Players.LocalPlayer

-- ===== CONFIG =====
local LOCK_DURATION = 0.4
local MIN_SPEED = 2
local enabled = false
local lockedVelocity = Vector3.zero
local lockTimer = 0
local connection = nil

-- ===== FIND THE BINDABLE EVENT =====
local bindableEvent = ReplicatedStorage:FindFirstChild("Effect") and ReplicatedStorage.Effect:FindFirstChild("Bindable")
if not bindableEvent then
    bindableEvent = ReplicatedStorage:WaitForChild("Effect"):WaitForChild("Bindable", 5)
    if not bindableEvent then
        warn("BindableEvent not found!")
    else
        print("Found BindableEvent!")
    end
end

-- ===== GUI =====
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "VelocityLockGUI"
screenGui.ResetOnSpawn = false
screenGui.Parent = player:WaitForChild("PlayerGui")

-- === FLOATING TOGGLE BUTTON ===
local toggleButton = Instance.new("TextButton")
toggleButton.Size = UDim2.new(0, 50, 0, 50)
toggleButton.Position = UDim2.new(0.9, -25, 0.1, 0)
toggleButton.BackgroundColor3 = Color3.fromRGB(60, 180, 60)
toggleButton.Text = "⚡"
toggleButton.TextColor3 = Color3.new(1, 1, 1)
toggleButton.Font = Enum.Font.GothamBold
toggleButton.TextSize = 28
toggleButton.Active = true
toggleButton.Parent = screenGui
Instance.new("UICorner", toggleButton).CornerRadius = UDim.new(0.5, 0)

-- === MAIN GUI ===
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 200, 0, 130)
mainFrame.Position = UDim2.new(0.5, -100, 0.3, 0)
mainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
mainFrame.BackgroundTransparency = 0.9
mainFrame.BorderSizePixel = 0
mainFrame.Active = true
mainFrame.Visible = false
mainFrame.Parent = screenGui
Instance.new("UICorner", mainFrame).CornerRadius = UDim.new(0, 10)

-- Title / drag handle
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 28)
title.BackgroundTransparency = 1
title.Text = "ROG ON TOP"
title.TextColor3 = Color3.fromRGB(255, 200, 100)
title.Font = Enum.Font.GothamBold
title.TextSize = 16
title.Active = true
title.Parent = mainFrame

-- Toggle ON/OFF button inside main GUI
local toggleBtn = Instance.new("TextButton")
toggleBtn.Size = UDim2.new(0.6, 0, 0, 30)
toggleBtn.Position = UDim2.new(0.2, 0, 0, 35)
toggleBtn.BackgroundColor3 = Color3.fromRGB(60, 180, 60)
toggleBtn.Text = "LOCK: OFF"
toggleBtn.TextColor3 = Color3.new(1, 1, 1)
toggleBtn.Font = Enum.Font.GothamBold
toggleBtn.TextSize = 14
toggleBtn.Active = true
toggleBtn.Parent = mainFrame
Instance.new("UICorner", toggleBtn).CornerRadius = UDim.new(0, 6)

-- Status label
local statusLabel = Instance.new("TextLabel")
statusLabel.Size = UDim2.new(1, 0, 0, 18)
statusLabel.Position = UDim2.new(0, 0, 0, 72)
statusLabel.BackgroundTransparency = 1
statusLabel.Text = "Status: Idle"
statusLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
statusLabel.Font = Enum.Font.Gotham
statusLabel.TextSize = 12
statusLabel.Parent = mainFrame

-- Ability label
local abilityLabel = Instance.new("TextLabel")
abilityLabel.Size = UDim2.new(1, 0, 0, 16)
abilityLabel.Position = UDim2.new(0, 0, 0, 92)
abilityLabel.BackgroundTransparency = 1
abilityLabel.Text = "Diamond M1 | Ghoul Grab"
abilityLabel.TextColor3 = Color3.fromRGB(150, 150, 180)
abilityLabel.Font = Enum.Font.Gotham
abilityLabel.TextSize = 10
abilityLabel.Parent = mainFrame

-- Debug label
local debugLabel = Instance.new("TextLabel")
debugLabel.Size = UDim2.new(1, 0, 0, 14)
debugLabel.Position = UDim2.new(0, 0, 0, 112)
debugLabel.BackgroundTransparency = 1
debugLabel.Text = ""
debugLabel.TextColor3 = Color3.fromRGB(100, 255, 100)
debugLabel.Font = Enum.Font.Gotham
debugLabel.TextSize = 9
debugLabel.Parent = mainFrame

-- ===== TOGGLE MAIN GUI VISIBILITY =====
local guiVisible = false
local buttonTap = false  -- used to distinguish tap from drag

local function toggleGUI()
    guiVisible = not guiVisible
    mainFrame.Visible = guiVisible
    toggleButton.Text = guiVisible and "✕" or "⚡"
    toggleButton.BackgroundColor3 = guiVisible and Color3.fromRGB(180, 60, 60) or Color3.fromRGB(60, 180, 60)
end

-- ===== DRAGGING FOR FLOATING BUTTON =====
local buttonDragging = false
local buttonDragStart = nil
local buttonStartPos = nil

local function startButtonDrag(input)
    buttonDragging = true
    buttonDragStart = input.Position
    buttonStartPos = toggleButton.Position
    buttonTap = true
end

local function endButtonDrag()
    buttonDragging = false
    buttonDragStart = nil
    buttonStartPos = nil
    -- If it was a tap (not a drag) we toggle GUI later in the InputEnded handler
end

local function updateButtonDrag(input)
    if buttonDragging and buttonDragStart and buttonStartPos then
        local delta = input.Position - buttonDragStart
        -- If movement > 10 pixels, it's a drag, not a tap
        if delta.Magnitude > 10 then
            buttonTap = false
        end
        toggleButton.Position = UDim2.new(
            buttonStartPos.X.Scale, buttonStartPos.X.Offset + delta.X,
            buttonStartPos.Y.Scale, buttonStartPos.Y.Offset + delta.Y
        )
    end
end

toggleButton.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        startButtonDrag(input)
    end
end)

toggleButton.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        -- If it was a tap (no drag), toggle GUI
        if buttonTap then
            toggleGUI()
        end
        endButtonDrag()
    end
end)

UserInputService.InputChanged:Connect(function(input)
    -- Update floating button drag
    if buttonDragging then
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            updateButtonDrag(input)
        end
    end
    -- Update main frame drag (if any)
    if mainFrameDragging then
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            updateMainFrameDrag(input)
        end
    end
end)

-- Also ensure drag ends if input is released anywhere
UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        if buttonDragging then
            if buttonTap then
                toggleGUI()
            end
            endButtonDrag()
        end
        if mainFrameDragging then
            endMainFrameDrag()
        end
    end
end)

-- ===== DRAGGING FOR MAIN FRAME =====
local mainFrameDragging = false
local mainDragStart = nil
local mainStartPos = nil

local function startMainFrameDrag(input)
    mainFrameDragging = true
    mainDragStart = input.Position
    mainStartPos = mainFrame.Position
end

local function endMainFrameDrag()
    mainFrameDragging = false
    mainDragStart = nil
    mainStartPos = nil
end

local function updateMainFrameDrag(input)
    if mainFrameDragging and mainDragStart and mainStartPos then
        local delta = input.Position - mainDragStart
        mainFrame.Position = UDim2.new(
            mainStartPos.X.Scale, mainStartPos.X.Offset + delta.X,
            mainStartPos.Y.Scale, mainStartPos.Y.Offset + delta.Y
        )
    end
end

title.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        startMainFrameDrag(input)
    end
end)

title.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        endMainFrameDrag()
    end
end)

-- ===== CORE LOGIC (unchanged) =====
local function resetLock()
    lockTimer = 0
    lockedVelocity = Vector3.zero
    statusLabel.Text = "Status: Idle"
    statusLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
end

local function triggerLock(abilityName)
    task.wait(0.05)
    local char = player.Character
    if not char then
        debugLabel.Text = "No char"
        return
    end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then
        debugLabel.Text = "No HRP"
        return
    end
    local currentVel = hrp.AssemblyLinearVelocity
    if currentVel.Magnitude > MIN_SPEED then
        lockedVelocity = currentVel
        lockTimer = LOCK_DURATION
        statusLabel.Text = "Locked: " .. string.format("%.0f", lockedVelocity.Magnitude) .. " studs/s"
        statusLabel.TextColor3 = Color3.fromRGB(255, 200, 100)
        debugLabel.Text = abilityName
        print("Velocity locked from", abilityName, "at", lockedVelocity.Magnitude)
    else
        debugLabel.Text = "Speed low: " .. string.format("%.1f", currentVel.Magnitude)
    end
end

local function listenForAbility()
    if not bindableEvent then return end
    bindableEvent.Event:Connect(function(eventType, container, data, metadata)
        if not enabled then return end
        if eventType ~= "spawn" or not container then return end
        local abilityName = container.Name
        local parent = container.Parent
        if parent and parent.Name ~= "Container" and parent.Name ~= "Effect" then
            abilityName = parent.Name .. "." .. abilityName
        end
        if abilityName == "DiamondRework.M1" or abilityName == "Ghoul.Grab" then
            task.spawn(function() triggerLock(abilityName) end)
        end
    end)
end

local function onLoop()
    local char = player.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    if lockTimer > 0 then
        hrp.AssemblyLinearVelocity = lockedVelocity
        hrp.AssemblyAngularVelocity = Vector3.zero
        lockTimer = lockTimer - 1/60
        if lockTimer <= 0 then
            resetLock()
        end
    end
end

local function startLoop()
    if connection then return end
    resetLock()
    connection = RunService.Heartbeat:Connect(onLoop)
end

local function stopLoop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    resetLock()
end

-- ===== TOGGLE LOCK =====
local function toggleLock()
    enabled = not enabled
    toggleBtn.Text = enabled and "LOCK: ON" or "LOCK: OFF"
    toggleBtn.BackgroundColor3 = enabled and Color3.fromRGB(180, 60, 60) or Color3.fromRGB(60, 180, 60)
    if enabled then
        startLoop()
        if bindableEvent then
            listenForAbility()
        else
            warn("No BindableEvent!")
        end
    else
        stopLoop()
    end
end

toggleBtn.Activated:Connect(toggleLock)

-- ===== KEYBIND (PC) =====
UserInputService.InputBegan:Connect(function(input, gp)
    if gp then return end
    if input.KeyCode == Enum.KeyCode.K then
        toggleLock()
    end
end)

-- ===== RESPAWN =====
player.CharacterAdded:Connect(function() end)

print("⚡ ROG ON TOP loaded. Tap ⚡ to open GUI, drag it to move.")
